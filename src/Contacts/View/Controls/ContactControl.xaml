<UserControl x:Class="View.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.ColumnDefinitions>
			<!-- Колонка под картинку -->
			<ColumnDefinition Width="Auto"/>
			<!-- Колонка под поля ввода -->
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<!-- Плейсхолдер для фото (100x100) -->
		<Border Grid.Column="0" Width="100" Height="100" Background="LightGray" Margin="3" VerticalAlignment="Top">
			<TextBlock Text="Photo" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
		</Border>

		<!-- Поля ввода справа -->
		<StackPanel Grid.Column="1">
			<!-- Имя -->
			<Label Content="Name:" Margin="3"/>
			<TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" 
                     Margin="3"/>

			<!-- Телефон -->
			<Label Content="Phone Number:" Margin="3"/>
			<TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" 
                     Margin="3"
                     PreviewTextInput="PhoneTextBox_PreviewTextInput"
                     DataObject.Pasting="PhoneTextBox_DataObjectPasting"/>

			<!-- Email -->
			<Label Content="Email:" Margin="3"/>
			<TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" 
                     Margin="3"/>
		</StackPanel>
	</Grid>
</UserControl>